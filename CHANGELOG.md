# Changelog

## v2.0.0 (2026-08-28)

- 修复窄屏语言菜单越界，并让菜单在视口边界自动翻转。
- 调整移动端信息顺序，棋盘优先进入首屏；首次访问默认静音。
- 限制全局键盘监听范围，避免按钮和菜单被方向键误操作。
- 新增 v3 本地存档信封、v2 兼容迁移和回放恢复一致性核验。
- 新增回放导入、完整事件重放校验和 IndexedDB v2 元数据迁移。
- 新增可按需切换的 Expert afterstate/N-tuple 实验引擎及并行基准脚本。
- 增加 PWA Manifest、离线 Service Worker、减少透明度/高对比度样式与实时棋盘公告。
- 将 Expert 升级为自适应实验引擎：前期使用 afterstate 评估，达到 256 或棋盘拥挤后切回 Search；补齐等节点预算与长短局发布门槛。
- 回放格式升级为 v4，在每个 AI 步骤记录所用引擎，同时兼容既有 v1-v3 日志。
- Service Worker 预缓存当前构建资源，并禁止用 HTML 壳错误响应缺失的脚本或样式请求。
- 将 AI 回退和活动回放会话从页面控制器拆分为独立模块；包版本与发布版本统一为 2.0.0。
